# import logging
#
# from selftf.agent import Agent
# from selftf.monitor import Monitor

if __name__ == "__main__":
    # logging.basicConfig(level=logging.DEBUG)
    #
    # agent = Agent(get_config())
    # agent.run()
    #
    # agent1 = Agent(get_config())
    # agent1.run()
    #
    # monitor = Monitor(get_config())
    # monitor.run()
    print(1/2)